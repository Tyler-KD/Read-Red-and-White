<div>
    <div>
        <h2 class="text-4xl text-black py-10 px-2 font-semibold">Welcome, {{username}}!</h2>
    </div>
</div>

<div>
    <div class="">
        <h3 class="text-center text-6xl text-black">Create a New Book Review</h3>
        <div class="flex-wrap mx-56 mt-6 text-center"><p1 class="text-2xl py-4 w-40">To write a review of a book you've read, start by recalling the book via the search below. Upon selection, summarize the book briefly, share your thoughts on its strengths and weaknesses, and offer recommendations. Be honest and respectful, aiming to provide helpful insights for potential readers.</p1></div>
        
        {{!-- Form for creating / posting a new book review --}}
        <form class="form new-review-form flex px-6 py-10">
            <div class="form-group  font-semibold text-4xl">
                {{!-- Book Review title form input --}}
                <label for="review-title text-center py-2">Search For a Book:</label>
                <input class="search-input border-2 rounded-lg flex justify-center my-6" type="text" id="review-title" name="review-title" />
                <button type="submit" class="search-btn rounded-lg border-2 border-black hover:scale-110 p-2 my-2 bg-blue-400 hover:bg-blue-600">Submit</button>
            </div>
        </form>
        <div class="flex justify-center text-3xl">
            <ul class="book-cards grid grid-cols-3 lg:grid-cols-3 md:grid-cols-2 sm:grid-cols-1 gap-14 px-6 justify-center" data-id={{id}}></ul>
        </div>
        
    </div>
    {{!-- Render Current Reviews for user --}}
    {{!-- handlebars for loop --}}
    <div class="text-4xl text-center font-semibold mt-8 pb-12">Titles Reviewed</div>
    <div class="flex-col mt-6 ">
        
        {{#if reviews.length}}
            <div class="review-list text-center text-4xl pb-12">
            {{!-- Built-in each helper iterates over reviews. --}}
            {{!-- Inside the block, you can use this to reference the element being iterated over. --}}
        {{#each reviews as |review|}}
            <div>
                <div>
                    <h4 class="py-4 hover:scale-110 hover:bg-slate-600"><a href="/review/{{review.id}}/edit">{{review.title}}</a></h4>
                </div>
            </div>
        {{/each}}
        </div>
        {{/if}}
    </div>
  
</div>

<script src="/js/bookapi.js"></script>
{{!-- <script src="/js/profile.js"></script> --}}