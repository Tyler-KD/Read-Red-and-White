<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Read Red and White</title>
    <link rel="stylesheet" href='/css/style.css'>
    {{! <script src="https://cdn.tailwindcss.com"></script> !}}
</head>


<body class="bg-gradient-to-r from-indigo-500 via-purple-500 to-pink-500 ...">
    <div>
        <header class="bg-purple-400 display-flex justify-space-between align-center p-6 py-6">
            <h1 class="bg-blue-600 text-8xl text-center font-semibold">Read Red and White</h1>
       
        </header>
        <nav class="bg-green-500 text-5xl text-center py-6 my-4">
                {{#if logged_in}}
                <a href="/" class="bg-blue-500 hover:bg-blue-700 text-white font-bold py-2 px-4 rounded-full">Home</a> |
                <a href="/profile" class="bg-blue-500 hover:bg-blue-700 text-white font-bold py-2 px-4 rounded-full">Profile</a> |
                <button class="bg-blue-500 hover:bg-blue-700 text-white font-bold py-2 px-4 rounded-full" id="logout">Logout</button>
                {{else}}
                <a href="/" class="bg-blue-500 hover:bg-blue-700 text-white font-bold py-2 px-4 rounded-full">Home</a> |
                <a href="/profile" class="bg-blue-500 hover:bg-blue-700 text-white font-bold py-2 px-4 rounded-full">Profile</a> |
                <a href="/login" class="bg-blue-500 hover:bg-blue-700 text-white font-bold py-2 px-4 rounded-full">Login</a>
                {{/if}}
        </nav>
        <div>
            <div>
             {{{ body }}}
            </div>
        </div>
    </div>
    {{!-- Render script for logged in users only --}}
    {{#if logged_in}}
    <script src="/js/logout.js"></script>
    {{/if}}
</body>

</html>