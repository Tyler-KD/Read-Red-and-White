<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    {{!-- Title in URL --}}
    <title>Read Red and White</title>
    <link rel="stylesheet" href='/css/style.css'>
    {{! <script src="https://cdn.tailwindcss.com"></script> !}}
</head>


<body class="bg-gradient-to-b from-red-800 via-purple-500 to-red-800">
    <div>
        <header class="bg-cover bg-center bg-fixed py-64" style="background-image: url('https://mshanken.imgix.net/wso/bolt/2020-04/stayinghome_books040620a_1600.jpg?auto=compress,format,&sharp=5&vib=20&q=70&w=1255');">
            {{!-- Title of page --}}
            <h1 class="text-8xl text-center font-semibold">Read <span class="text-red-800">Red</span> and <span class="text-white">White</span></h1>
       

                <nav class= "text-5xl text-center py-6 my-4">
                    {{#if logged_in}}
                    <a href="/" class="bg-red-700 hover:bg-black bg-opacity-40 hover:opacity-100 text-white font-bold py-2 px-4 rounded-full">Home</a> |
                    <a href="/profile" class="bg-black hover:bg-black bg-opacity-40 hover:opacity-100 text-white font-bold py-2 px-4 rounded-full">Profile</a> |
                    <a class="bg-red-700 hover:bg-black bg-opacity-40 hover:opacity-100 text-white font-bold py-2 px-4 rounded-full" id="logout">Logout</a>
                    {{else}}
                    <a href="/" class=" bg-red-700 hover:bg-black bg-opacity-40 hover:opacity-100 text-white font-bold py-2 px-4 rounded-full">Home</a> |
                    <a href="/profile" class="bg-white hover:bg-white bg-opacity-40 hover:opacity-100 text-black font-bold py-2 px-4 rounded-full">Profile</a> |
                    <a href="/login" class=" bg-red-700 hover:bg-black bg-opacity-40 hover:opacity-100 text-white font-bold py-2 px-4 rounded-full">Login</a>
                    {{/if}}
                </nav>

        </header>
   
        <div>
            <div>
             {{{ body }}}
            </div>
        </div>
    </div>
    {{!-- Render script for logged in users only --}}
    {{#if logged_in}}
    <script src="/js/logout.js"></script>
    {{/if}}
</body>

</html>